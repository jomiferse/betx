package com.betx.application;

public enum DiagnosticsStakeSizingRankingEligibility {
    ELIGIBLE,
    INSUFFICIENT_SAMPLE,
    NO_EXPOSURE,
    ALL_BLOCKED,
    SHADOW_ONLY,
    INVALID_DATA,
    HIGH_RISK
}
